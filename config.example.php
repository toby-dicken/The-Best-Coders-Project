<?php
// Copy the contents of this file to config.php and fill in your real credentials.
// Do NOT commit the real config.php, that is a security risk (though config.php is in gitignore now, so it shouldn't commit).

define('DB_HOST', 'localhost');
define('DB_USER', 'app_user');     // avoid root
define('DB_PASS', 'change_me');
define('DB_NAME', 'db_name');
